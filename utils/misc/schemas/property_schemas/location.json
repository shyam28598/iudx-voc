{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "<iudx>/location",
    "title": "location",
    "describes": "Location associated with a resource Item (a spatial point), a group of resource items (a spatial region) or even an observation in case of a mobile sensor.",
    "oneOf": [
        {
            "$ref": "<iudx>/Place"
        },
        {
            "$ref": "<iudx>/Text"
        }
    ]
}