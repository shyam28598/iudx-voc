{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "<iudx>/minDurationInHours",
    "title": "minDurationInHours",
    "describes": "Specifies the interval duration in Hours over which minimum value is computed. This is used in case the minimum value is reported over all the measurements in a given duration.",
    "$ref": "<iudx>/Number"
}